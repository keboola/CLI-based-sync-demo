name: Keboola Secret Validation
description: Pulling from Keboola both env projects configurations and search existing secrets
runs:
  using: "composite"
  steps:
    - env:
        kbcProjectId: ${{ vars.KBC_PROJECT_ID_L0 }}
      run: |
        "echo" "Hello ! while Keboola secret validation $kbcProjectId"
      shell: bash
